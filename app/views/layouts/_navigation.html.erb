<%= link_to "Memoir", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#">
        <%= current_user.name %>
        <b class="caret"></b>
      </a>
      <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
        <li><%= link_to 'Logout', signout_path %></li>
      </ul>
    </li>
  <% else %>
    <li>
      <%= link_to 'Login', signin_path %>
    </li>
  <% end %>
</ul>
